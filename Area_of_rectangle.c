#include <stdio.h>
int rec(int 1,int b)
{
int area=1*b;
return area;
}
int main()
int 1,b;
printf("Enter the value of 1 and b: ");
scanf("%d %d ",&l,&b);
int res=rec(1,b);
printf("\nArea of rectangle: %d",res);
return 0;
}